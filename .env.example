HYSTORIA_USERNAME=monuser
HYSTORIA_PASSWORD=monmdp
VOTE_INTERVAL_MINUTES=90
HEADLESS=true
BASE_URL=https://play-hystoria.net

# dofus_bot (MITM proxy)
# Local proxy address your Dofus client must connect to.
DOFUS_PROXY_HOST=127.0.0.1
DOFUS_PROXY_PORT=5555
# Real Hystoria game server the proxy forwards to.
# IP identified via netstat while the Dofus Retro Hystoria V5 client is
# connected - confirmed on 2026-04-09. If it changes, run:
#   netstat -n | findstr :5555
# while the client is at the character-select screen and update this line.
DOFUS_UPSTREAM_HOST=162.19.127.155
DOFUS_UPSTREAM_PORT=5555
# Optional Lua script (relative to dofus_bot/data/scripts/)
DOFUS_SCRIPT=
DOFUS_LOG_LEVEL=INFO
